import { GamesQuantityPipe } from './games-quantity.pipe';

describe('GamesQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new GamesQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
