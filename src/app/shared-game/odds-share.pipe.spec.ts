import { OddsSharePipe } from './odds-share.pipe';

describe('OddsSharePipe', () => {
  it('create an instance', () => {
    const pipe = new OddsSharePipe();
    expect(pipe).toBeTruthy();
  });
});
